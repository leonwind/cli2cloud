syntax = "proto3";

option go_package = "github.com/leonwind/service/servicepb";

package servicepb;

service Cli2Cloud {
  rpc Publish(stream Content) returns (Empty);
  rpc Subscribe(Empty) returns (stream Content);
}

message Content {
  string Payload = 1;
}

message Empty {}